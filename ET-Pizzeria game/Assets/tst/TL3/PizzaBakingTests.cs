using NUnit.Framework;
using UnityEngine;
/*

public class TL3PizzaBakingTests
{
    [Test]
    public void PizzaStartsRaw()
    {
        GameObject obj = new GameObject();
        PizzaBaking baking = obj.AddComponent<PizzaBaking>();

        Assert.IsFalse(baking.isCooked);
        Assert.IsFalse(baking.isBurnt);

        Object.DestroyImmediate(obj);
    }

    [Test]
    public void PizzaBecomesCooked()
    {
        GameObject obj = new GameObject();
        PizzaBaking baking = obj.AddComponent<PizzaBaking>();

        baking.SetBakeTime(10f);

        Assert.IsTrue(baking.isCooked);

        Object.DestroyImmediate(obj);
    }

    [Test]
    public void PizzaBecomesBurnt()
    {
        GameObject obj = new GameObject();
        PizzaBaking baking = obj.AddComponent<PizzaBaking>();

        baking.SetBakeTime(20f);

        Assert.IsTrue(baking.isBurnt);

        Object.DestroyImmediate(obj);
    }
}
*/